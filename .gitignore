tags
test.sh
.luarc.json
nvim

spell/

# u likely want to remove lazy-lock.json from your fork's .gitignore file too - 
# it's ignored in the kickstart repo to make maintenance easier, 
# but it's recommended to track it in version control.
# lazy-lock.json 
